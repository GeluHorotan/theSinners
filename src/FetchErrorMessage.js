import React from 'react';

const FetchErrorMessage = () => {
  return (
    <div className='middle_align' style={{ color: '#fff' }}>
      We're sorry, it looks like something went wrong. Please refresh the page.
    </div>
  );
};

export default FetchErrorMessage;
